<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head') %>
  <title>Provider Profile | <%= provider.FullName %></title>
</head>
<body>
  <div class="container py-5">
    <div class="card shadow p-4">
      <h2 class="text-primary mb-3"><%= provider.FullName %></h2>
      <p><strong>Service Type:</strong> <%= provider.ServiceType %></p>
      <p><strong>Experience:</strong> <%= provider.Experience %> years</p>
      <p><strong>Description:</strong> <%= provider.Description %></p>
      <p><strong>Availability:</strong> <%= provider.Availability %></p>
      <p><strong>Rating:</strong> <%= provider.Rating %> / 5 ⭐</p>

      <hr>
      <h5 class="mt-4 mb-3">Reviews</h5>
      <% if (reviews.length === 0) { %>
        <p class="text-muted">No reviews available.</p>
      <% } else { %>
        <ul class="list-group">
          <% reviews.forEach(review => { %>
            <li class="list-group-item">
              <strong><%= review.Rating %>★</strong> - <%= review.Comment %><br>
              <small class="text-muted"><%= new Date(review.CreatedAt).toLocaleDateString() %></small>
            </li>
          <% }) %>
        </ul>
      <% } %>
    </div>
  </div>
</body>
</html>
